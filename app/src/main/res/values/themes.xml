<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProfitPortfolio" parent="Theme.AppCompat.Light">

        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/appBars</item>
        <item name="titleTextColor">@color/white</item>

    </style>

    <style name="Base.Theme.AddStock" parent="Theme.MaterialComponents.Light.Bridge">
        <item name="colorPrimary">@color/appBars</item>
        <item name="titleTextColor">@color/white</item>

    </style>
    <style name="Base.Theme.DetailsStock" parent="Theme.MaterialComponents.Light.Bridge">
        <item name="colorPrimary">@color/appBars</item>
        <item name="titleTextColor">@color/white</item>

    </style>

    <style name="Base.Theme.BuyActivity" parent="Theme.MaterialComponents.Light.Bridge">
        <item name="colorPrimary">@color/appBars</item>
        <item name="titleTextColor">@color/white</item>

    </style>
    <style name="Base.Theme.SellActivity" parent="Theme.MaterialComponents.Light.Bridge">
        <item name="colorPrimary">@color/appBars</item>
        <item name="titleTextColor">@color/white</item>

    </style>
</resources>